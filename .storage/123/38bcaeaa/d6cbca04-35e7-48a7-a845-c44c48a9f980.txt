$ cd /workspace/shadcn-ui && cat package.json | grep -A 5 "scripts"
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint --quiet ./src",
    "preview": "vite preview"
  },
